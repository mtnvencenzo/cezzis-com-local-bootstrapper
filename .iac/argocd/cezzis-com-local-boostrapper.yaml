apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cezzis-com-local-bootstrapper
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: cezzis
spec:
  project: default
  source:
    repoURL: 'https://github.com/mtnvencenzo/cezzis-com-local-bootstrapper.git'
    targetRevision: main
    path: .iac/k8s
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: cezzis
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - Replace=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    # 1. Ignore the image tag updates on your Job
    - group: ""
      kind: Job
      jsonPointers:
        - /spec/template/spec/containers/0/image
    
    # 2. Ignore the structural normalization on your ImageUpdater
    - group: argocd-image-updater.argoproj.io
      kind: ImageUpdater
      name: cezzis-com-local-bootstrapper-updater
      namespace: cezzis
      jqPathExpressions:
        # Ignore where you have it in Git
        - .spec.useAnnotations
        # Ignore where the controller moved it in the Cluster
        - .spec.applicationRefs[].useAnnotations
        # Ignore the injected default
        - .spec.applicationRefs[].commonUpdateSettings.forceUpdate
        # Ignore the field that disappeared in the Cluster
        - .spec.applicationRefs[].commonUpdateSettings.versionConstraint